import { motion } from 'framer-motion'; import { skillGroups } from '../../data/portfolio';
export function Skills() { return <section id="skills" className="section border-y border-white/[.07] bg-white/[.018]"><div className="container-page"><motion.div initial={{ opacity: 0, y: 14 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .25 }} transition={{ duration: .55 }}><p className="eyebrow">Capabilities</p><h2 className="section-title">A growing technical toolkit.</h2><p className="section-copy">Tools and subjects I’m actively developing through focused learning and hands-on practice.</p></motion.div><div className="mt-10 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{skillGroups.map(([group, skills], index) => <motion.article initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .15 }} transition={{ duration: .4, delay: index * .04 }} className="surface glass-hover p-5" key={group}><h3 className="font-semibold text-white">{group}</h3><div className="mt-4 flex flex-wrap gap-2">{skills.map((skill) => <span key={skill} className="rounded-lg border border-white/[.09] bg-white/[.04] px-3 py-1.5 text-sm text-slate-300">{skill}</span>)}</div></motion.article>)}</div></div></section>; }
