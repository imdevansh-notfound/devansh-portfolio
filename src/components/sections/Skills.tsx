import { skillGroups } from '../../data/portfolio';
export function Skills() { return <section id="skills" className="section"><div className="container-page"><p className="eyebrow">Capabilities</p><h2 className="section-title">A growing technical toolkit.</h2><p className="section-copy">Tools and subjects I’m actively developing through focused learning and hands-on practice.</p><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{skillGroups.map(([group, skills]) => <article className="surface p-6" key={group}><h3 className="font-semibold">{group}</h3><div className="mt-4 flex flex-wrap gap-2">{skills.map((skill) => <span key={skill} className="rounded-full border border-gray-200 bg-white px-3 py-1.5 text-sm text-muted dark:border-slate-700 dark:bg-slate-950 dark:text-slate-300">{skill}</span>)}</div></article>)}</div></div></section>; }
