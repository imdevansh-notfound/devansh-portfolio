import { Code2, ExternalLink } from 'lucide-react'; import { useState } from 'react'; import { projects } from '../../data/portfolio';
const filters = ['All', 'Web', 'AI', 'Mobile'] as const;
export function Projects() { const [filter, setFilter] = useState<(typeof filters)[number]>('All'); const selected = filter === 'All' ? projects : projects.filter((project) => project.category === filter); return <section id="projects" className="section border-y border-gray-100 dark:border-slate-900"><div className="container-page"><p className="eyebrow">Selected work</p><h2 className="section-title">Projects, ready for your story.</h2><p className="section-copy">These intentionally transparent placeholders are ready to be replaced with your shipped work—without claiming work you haven’t done.</p><div className="mt-8 flex flex-wrap gap-2">{filters.map((item) => <button className={'focus-ring rounded-full px-4 py-2 text-sm font-semibold transition ' + (filter === item ? 'bg-blue-600 text-white' : 'border border-gray-200 text-muted hover:bg-gray-50 dark:border-slate-700 dark:hover:bg-slate-900')} key={item} onClick={() => setFilter(item)}>{item}</button>)}</div><div className="mt-8 grid gap-5 lg:grid-cols-3">{selected.map((project) => <article className="surface overflow-hidden" key={project.title}><div className="flex aspect-[16/9] items-center justify-center border-b border-gray-200 bg-gradient-to-br from-blue-50 to-gray-100 dark:border-slate-800 dark:from-slate-900 dark:to-slate-800"><span className="font-mono text-xs font-semibold uppercase tracking-[.18em] text-blue-700 dark:text-blue-300">Project preview</span></div><div className="p-6"><div className="flex items-start justify-between gap-3"><h3 className="font-['Poppins'] text-lg font-semibold">{project.title}</h3><span className="shrink-0 rounded-full bg-blue-50 px-2.5 py-1 text-xs font-medium text-blue-700 dark:bg-blue-950 dark:text-blue-300">{project.status}</span></div><p className="mt-3 text-sm leading-6 text-muted dark:text-slate-400">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.tags.map((tag) => <span key={tag} className="text-xs font-medium text-muted">{tag}</span>)}</div><div className="mt-5 flex gap-4 text-sm font-semibold"><a className="focus-ring inline-flex items-center gap-1.5 text-muted hover:text-accent" href={project.repo ?? '#projects'}>Code <Code2 size={15}/></a><a className="focus-ring inline-flex items-center gap-1.5 text-muted hover:text-accent" href={project.demo ?? '#projects'}>Demo <ExternalLink size={15}/></a></div></div></article>)}</div></div></section>; }
