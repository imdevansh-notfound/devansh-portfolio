import { motion } from 'framer-motion'; import { Code2, ExternalLink } from 'lucide-react'; import { useState } from 'react'; import { projects } from '../../data/portfolio';
const filters = ['All', 'Web', 'AI', 'Mobile'] as const;
export function Projects() { const [filter, setFilter] = useState<(typeof filters)[number]>('All'); const selected = filter === 'All' ? projects : projects.filter((project) => project.category === filter); return <section id="projects" className="section"><div className="container-page"><motion.div initial={{ opacity: 0, y: 14 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .25 }} transition={{ duration: .55 }}><p className="eyebrow">Selected work</p><h2 className="section-title">Projects, ready for your story.</h2><p className="section-copy">These intentionally transparent placeholders are ready to be replaced with your shipped work—without claiming work you haven’t done.</p></motion.div><div className="mt-8 flex flex-wrap gap-2">{filters.map((item) => <button className={'focus-ring rounded-lg border px-4 py-2 text-sm font-semibold transition ' + (filter === item ? 'border-blue-400/60 bg-blue-500/15 text-blue-200' : 'border-white/10 bg-white/[.03] text-slate-400 hover:bg-white/[.08] hover:text-white')} key={item} onClick={() => setFilter(item)}>{item}</button>)}</div><div className="mt-8 grid gap-5 lg:grid-cols-3">{selected.map((project, index) => <motion.article initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .15 }} transition={{ duration: .45, delay: index * .07 }} className="surface glass-hover overflow-hidden" key={project.title}><div className="flex aspect-[16/9] items-end border-b border-white/10 bg-[radial-gradient(circle_at_25%_20%,rgba(59,130,246,.22),transparent_35%),linear-gradient(135deg,rgba(30,41,59,.8),rgba(15,23,42,.65))] p-5"><span className="font-mono text-[11px] font-semibold uppercase tracking-[.2em] text-blue-200/80">Project preview</span></div><div className="p-6"><div className="flex items-start justify-between gap-3"><h3 className="font-['Poppins'] text-lg font-semibold text-white">{project.title}</h3><span className="shrink-0 rounded-full border border-blue-300/20 bg-blue-400/10 px-2.5 py-1 text-xs font-medium text-blue-200">{project.status}</span></div><p className="mt-3 text-sm leading-6 text-slate-400">{project.description}</p><div className="mt-5 flex flex-wrap gap-x-3 gap-y-1">{project.tags.map((tag) => <span key={tag} className="text-xs font-medium text-slate-300">{tag}</span>)}</div><div className="mt-5 flex gap-4 text-sm font-semibold"><a className="focus-ring inline-flex items-center gap-1.5 text-slate-400 hover:text-blue-300" href={project.repo ?? '#projects'}>Code <Code2 size={15}/></a><a className="focus-ring inline-flex items-center gap-1.5 text-slate-400 hover:text-blue-300" href={project.demo ?? '#projects'}>Demo <ExternalLink size={15}/></a></div></div></motion.article>)}</div></div></section>; }
