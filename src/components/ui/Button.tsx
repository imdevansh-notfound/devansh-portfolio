import type { ButtonHTMLAttributes, ReactNode } from 'react';
import { cn } from '../../lib/cn';
type Props = ButtonHTMLAttributes<HTMLButtonElement> & { children: ReactNode; variant?: 'primary' | 'secondary' | 'ghost' };
export function Button({ children, className, variant = 'primary', ...props }: Props) { return <button className={cn('focus-ring shine inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition duration-200 active:scale-[.98]', variant === 'primary' && 'bg-blue-500 text-white shadow-[0_8px_25px_rgba(37,99,235,.25)] hover:bg-blue-400', variant === 'secondary' && 'border border-white/15 bg-white/[.07] text-white hover:bg-white/[.12]', variant === 'ghost' && 'text-slate-300 hover:bg-white/[.08]', className)} {...props}>{children}</button>; }
