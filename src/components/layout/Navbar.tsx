import { Menu, Moon, Sun, X } from 'lucide-react'; import { useState } from 'react'; import { Link } from 'react-router-dom'; import { useTheme } from '../../hooks/useTheme'; import { Button } from '../ui/Button';
const nav = ['About', 'Skills', 'Projects', 'Education', 'Contact'];
export function Navbar() { const [open, setOpen] = useState(false); const { dark, toggle } = useTheme(); return <header className="sticky top-0 z-50 border-b border-gray-100 bg-white/85 backdrop-blur dark:border-slate-900 dark:bg-slate-950/85"><nav className="container-page flex h-16 items-center justify-between"><Link className="focus-ring font-['Poppins'] font-bold tracking-tight" to="/">DT<span className="text-accent">.</span></Link><div className="hidden items-center gap-6 md:flex">{nav.map((item) => <a key={item} className="focus-ring text-sm font-medium text-muted transition hover:text-ink dark:hover:text-white" href={'/#' + item.toLowerCase()}>{item}</a>)}<button aria-label="Toggle dark mode" className="focus-ring rounded-md p-2" onClick={toggle}>{dark ? <Sun size={18}/> : <Moon size={18}/>}</button></div><div className="flex items-center md:hidden"><button aria-label="Toggle dark mode" className="focus-ring mr-2 rounded-md p-2" onClick={toggle}>{dark ? <Sun size={18}/> : <Moon size={18}/>}</button><Button aria-label="Open navigation" variant="ghost" className="p-2" onClick={() => setOpen(!open)}>{open ? <X/> : <Menu/>}</Button></div></nav>{open && <div className="border-t border-gray-100 bg-white px-5 py-4 dark:border-slate-800 dark:bg-slate-950 md:hidden">{nav.map((item) => <a onClick={() => setOpen(false)} key={item} className="focus-ring block rounded-md px-3 py-3 font-medium" href={'/#' + item.toLowerCase()}>{item}</a>)}</div>}</header>; }
